name: vesuvivus_pos
description: Vesuvivus waiter POS (MVVM)
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  provider: ^6.1.2
  shared_preferences: ^2.2.3
  laravel_echo: ^1.0.0-beta.1
  pusher_channels_flutter: ^2.5.0
  onesignal_flutter: ^5.1.0
  firebase_core: ^2.25.4

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
